<?xml version="1.0" encoding="UTF-8" ?>
<ATest xmlns="http://schema.Hyperdrive.tech/ui"
    xmlns:layout="http://schema.Hyperdrive.tech/layout"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://schema.Hyperdrive.tech/ui http://schema.Hyperdrive.tech/ui.xsd
                        http://schema.Hyperdrive.tech/layout http://schema.Hyperdrive.tech/layout.xsd"
    type="ATest"
    extend="all"
    anonymous="true"
    rootView="true"
    backgroundColor="$testColor">

    <!-- <state
        image="falcon"
        /> -->

<!-- enum ATest.Action {
    case otherAction(OtherView.Action)

    case send(h: String, f: Int, index: IndexPath)
    case hello
}

    <OtherView
        action="otherAction"

        action:doSomething="send(..., index: @tableView.selectedIndex)"
         />

enum OtherView.Action {
    case send
    case doSomething(h: String, f: Int)
} -->
    <ExampleCell
        id="example"
        export="true"
        title="OK"
        type="ExampleCell"
        />
        <!-- action:tap="cellTapped(..., im: $image, @falcon.image, ref: @very.far.far.away, test: String(dude), empty: String(), $message, Int(0), ...)" -->

    <ImageView
        id="falcon"
        action:tap="batman"
        layout:edges="super"
        backgroundColor="$imageColor"
        image="$image"
        visibility="visible"
        clipsToBounds="true"
        layout:compressionPriority="high"
        layout:huggingPriority="low"
        contentMode="scaleAspectFill" />

    <Label
        text="$message"
        />

    <ImageView
        image="$image"
        />

    <ImageView
        image="$image"
        />

<Container>
        <ImageView
            image="$image"
            />

            <ImageView
                image="$image"
                />
</Container>
    <PlainTableView
        cell="ExampleCell3"
        layout:edges="super"
        examples="5">

        <cell anonymous="true">
            <Label
                text="This is nice"
                backgroundColor="white"
                textColor="red"
                layout:fillHorizontally="super inset(20)"
                layout:fillVertically="super inset(0)" />
        </cell>
    </PlainTableView>
</ATest>
